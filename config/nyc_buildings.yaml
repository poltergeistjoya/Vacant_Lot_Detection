# NYC configuration for vacant lot detection - Residential buildings analysis
# Only specify values that differ from defaults (see config.py for defaults)

city: nyc
city_name: New York City
run_name: buildings

# City geometry (required - defines the geographic extent)
geometry:
  source: tiger_counties
  state_fips: "36"  # New York state
  counties:
    - New York    # Manhattan
    - Kings       # Brooklyn
    - Queens
    - Bronx
    - Richmond    # Staten Island

# Parcel data source (required - no defaults for paths)
parcel:
  data_path: data/nyc_mappluto_22v3_arc_fgdb/MapPLUTO22v3.gdb
  layer: MapPLUTO_22v3_clipped
  id_column: BBL
  landuse_column: BldgClass
  vacant_codes:
    - "G7"  # Unlicensed parking lot
    - "V1" # all other vacant building codes
    - "V9" 
    - "V5" 
    - "V4" 
    - "V7" 
    - "V8" 
    - "V6" 
    - "V0" 
    - "V3" 
    - "V2"

  source_crs: EPSG:2263

raster:
  output_crs: EPSG:32618

# GEE overrides (optional - uses auto-generated defaults if not specified)
gee:
  export_prefix: eda/new_york_new_york_bldgclss  # Override the default "nyc"

# NAIP year (other fields auto-generated from year)
naip:
  year: 2022

# Sampling parameters (using defaults but can override)
sampling:
  vacant_min_fraction: 0.02  # Lower since these aren't truly "vacant"

segmentation:
  bbox: [-74.27, 40.49, -73.70, 40.92]  # NYC 5-borough bounding box (WGS84)
